{{if eq .Code "workspace_config_missing"}}从备份恢复 workspace.json，或在使用 PUA 前重新构建该文件。{{else if eq .Code "workspace_config_invalid"}}手工修复 workspace.json，或从备份恢复。{{else if eq .Code "workspace_version_unsupported"}}使用兼容版本的 PUA 迁移 Workspace。{{else if eq .Code "workspace_language_invalid"}}将 language 设置为 en 或 zh-CN。{{else if eq .Code "workspace_instance_id_missing"}}运行 pua migrate 生成稳定的 Workspace instance ID。{{else if eq .Code "workspace_scan_failed"}}恢复配置的 Workspace 路径及读取权限。{{else if eq .Code "agent_binding_invalid"}}选择有效的 Profile 或 Agent 绑定。{{else if eq .Code "workspace_unreadable"}}恢复 Workspace 的读取权限。{{else if eq .Code "project_path_invalid"}}检查内容后，将该路径替换为普通目录。{{else if eq .Code "project_metadata_missing"}}从备份恢复 project.json，或重建 Project 元数据。{{else if eq .Code "resource_metadata_conflict"}}检查两个文件后移除错误的元数据文件。{{else if eq .Code "project_metadata_invalid"}}手工修复 project.json，或从备份恢复。{{else if eq .Code "project_directory_mismatch"}}将目录重命名为规范的 projectN[-slug] 形式。{{else if eq .Code "project_unreadable"}}恢复 Project 目录的读取权限。{{else if eq .Code "task_path_invalid"}}检查内容后，将该路径替换为普通目录。{{else if eq .Code "task_metadata_missing"}}从备份恢复 task.json，或重建 Task 元数据。{{else if eq .Code "task_metadata_invalid"}}手工修复 task.json，或从备份恢复。{{else if eq .Code "task_directory_mismatch"}}将目录重命名为规范的 taskN[-slug] 形式。{{else if eq .Code "task_parent_mismatch"}}修正 parent，或将 Task 移到规范 Project 中。{{else if eq .Code "task_template_missing"}}如果后续可复现性仍然重要，请恢复该模板。{{else if eq .Code "task_template_invalid"}}修复 Project 模板。{{else if eq .Code "templates_unreadable"}}恢复 templates 目录的读取权限。{{else if eq .Code "template_name_invalid"}}使用字母、数字、点、下划线或连字符重命名模板。{{else if eq .Code "template_unreadable"}}恢复模板的读取权限。{{else if eq .Kind "template_validation"}}修复模板后再用它创建 Task。{{else if eq .Code "task_worktree_storage_external"}}让 Task worktree 基于 repos/ 下的仓库。{{else if eq .Code "task_worktree_storage_unsafe"}}让 Task worktree 基于 repos/ 下的仓库。{{else if eq .Code "task_worktree_storage_not_found"}}恢复仓库，或重新创建 worktree。{{else if eq .Code "task_worktree_storage_invalid"}}修复或替换仓库 checkout。{{else if eq .Code "scheduler_config_invalid"}}手工修复 scheduler.json，或从备份恢复。{{else if eq .Code "schedule_target_missing"}}修正 target，或移除该 schedule。{{else if eq .Code "agent_catalog_unavailable"}}启动或连接拥有当前 Workspace 的 pua serve，然后重新运行 doctor。{{else if eq .Code "agent_profile_missing"}}选择已配置的 Agent Profile。{{else if eq .Code "agent_binding_target_missing"}}选择现有且可用的 Agent。{{else if eq .Code "agent_profile_target_missing"}}修复 Profile 配置，指向现有且可用的 Agent。{{else if eq .Code "agent_target_unavailable"}}修复 Provider，或选择其他 Agent。{{else if eq .Code "agents_file_missing"}}恢复必需的 AGENTS.md 文件。{{else if eq .Code "agents_file_unreadable"}}恢复 AGENTS.md 的读取权限。{{else if eq .Code "agents_managed_section_missing"}}检查本地指引后运行 pua migrate。{{else if eq .Code "agents_managed_markers_invalid"}}修复托管标记，然后运行 pua migrate。{{else if eq .Code "agents_managed_section_modified"}}检查托管区块外的本地指引后运行 pua migrate。{{else if eq .Code "markdown_file_missing"}}如果仍需要该资源上下文，请恢复文件。{{else if eq .Code "markdown_file_invalid"}}将该路径替换为普通 Markdown 文件。{{else if eq .Code "resource_timestamp_invalid"}}修正资源时间戳。{{else if eq .Code "resource_timestamp_order_invalid"}}如果不是有意如此，请修正资源时间戳。{{else if eq .Code "resource_id_duplicate"}}只保留一个使用该 ID 的开放资源。{{else if eq .Code "required_file_missing"}}恢复必需文件。{{else if eq .Code "required_file_unreadable"}}恢复必需文件的访问权限。{{else if eq .Code "required_file_invalid"}}将它替换为普通文件。{{else if eq .Code "required_directory_missing"}}检查 Workspace 数据后恢复或重建该目录。{{else if eq .Code "required_directory_invalid"}}将该路径替换为普通目录。{{else}}{{.Original}}{{end}}
