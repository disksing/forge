`{{.ResourceID}}` 的 Turn 结果：generation `{{.GenerationID}}` 的 Turn `{{.TurnID}}` 以状态 `{{.Status}}` 结束。{{if .SourceMessageIDs}}

来源消息 ID：`{{.SourceMessageIDs}}`{{end}}{{if .Reference}}

Turn 引用：`{{.Reference}}`{{end}}{{if .Reply}}

Agent 最终回复：

{{.Reply}}{{else if .HistoryUnavailable}}

已记录终态结果，但规范 Turn 历史不可用，因此没有生成 Turn 引用。{{else}}

该 Turn 没有最终 Agent 文本。{{end}}
